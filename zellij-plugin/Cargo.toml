[package]
name = "cw-status"
version = "0.1.0"
edition = "2021"
description = "Zellij status bar plugin for claude-workspace"

[dependencies]
zellij-tile = "0.41"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib"]
